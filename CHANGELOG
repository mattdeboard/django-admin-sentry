0.2.2

* Users with no log entries in the system are no longer be represented in the User filter widget.

0.2.1

* Fixed broken link assigned to usernames on log entries.

0.2

* Superusers can now log into and out of admin_sentry (and their app's admin interface) via /admin_sentry/[login|logout].

0.1

* Initial release